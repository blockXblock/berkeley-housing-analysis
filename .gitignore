# Python
*.pyc
__pycache__/
.ipynb_checkpoints/

# System
.DS_Store

# Data (keep only final)
permits_raw.csv
housing_projects_filtered.csv
*_refreshed_*.csv

# Generated outputs
*.db
outputs/*.html
outputs/*.md
data/outputs/
data/backups/
databases/
output/D5/
output/D6/
notebooks/06_polygon_cleanup/reports/

# Scrape outputs and run artifacts (regenerable from scripts in scripts/)
data/raw/accela_inspections/
data/raw/accela_processing_status/
data/raw/accela_record_status/
data/raw/accela_status/
data/raw/accela_url_discovery/
logs/

# Audit run outputs (regenerable from scripts in analysis/audit_2026-05-16/)
analysis/audit_2026-05-16/*.txt
analysis/audit_2026-05-16/*.csv

# Accela scrape experiment HTML responses (large; regenerable)
experiments/accela_scrape/response_*.html

# Letter drafts (keep private; use docs/letters/sent_log.md for metadata)
docs/letters/*.md
!docs/letters/sent_log.md

# Keep structure
!outputs/.gitkeep

berkeleyshops-audience/
hello-fly/
.obsidian/

# Obsidian per-user state
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Large binary exports
*.zip
*.m4v
*.mov
*.mp4

# Exception: deployed tour videos (compressed for web)
!docs/videos/*.mp4

# Tour source recordings (large uncompressed files, keep local only)
data/raw/tour_recordings/*.m4v
data/raw/tour_recordings/*.mov
data/raw/tour_recordings/*-source.mp4

# PDFs — managed via document mirror subsystem (IA + R2), not git
pdf/
*.pdf

# OS cruft
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# SQLite auxiliary files
*.db-journal
*.db-wal
*.db-shm

# Pending review — do not commit until notebook is reconciled
notebooks/MASTER_ANALYSIS.ipynb

# Accela authentication cookies (never commit)
experiments/accela_scrape/cookies.txt

# Inspection scraper output — machine-generated, regenerated by future runs
data/raw/accela_inspections/

# URL discovery scraper output — machine-generated, regenerated by future runs
data/raw/accela_url_discovery/

# Prune-pile / clutter (snapshot DBs already covered by databases/ above)
energy/
output/
.claude/*
!.claude/commands/
*.bak
.env.r2
*.env.r2
# Video outputs live on the YouTube channel feeding berkeleybuild.com, not the repo
*.mp4
*.mp4.backup*
# Scratch notes
/2026-05-28.md
notes/cc_prompts/
# Persistent scratch — survives reboots (unlike /tmp), git-invisible
scratch/

# Calibration prototypes — kept on disk, not committed (standing decision)
scripts/v4/prototype_score*.py
# Packaged module tarball (derived; source modules are tracked)
*.tar.gz

# Curriculum skip-ingest cache — derived from tracked raw CPRA xlsx by JN1
# (USE_CLEAN toggle; permits_clean_README.md documents regeneration)
data/processed/permits_clean.csv
data/processed/permits_clean.parquet

# Generated by JN-A at execution (BASE-state verifier emitted by the notebook run)
scripts/verify_jn_a_base_conservation.py

# published public mirror (the ONE deliberately-public database copy; see docs/reporters.html)
!docs/data/berkeley_housing_v2_public.db
experiments/accela_scrape/debug_dr/

# Infrastructure raw GeoJSON pulls (~68 MB, re-fetchable in minutes).
# Regenerate: scripts/infrastructure/pull_arcgis_layer.py <layer_url> <out.geojson>
# The layer inventory + OSM comparison JSONs ARE tracked — they are the small,
# citable catalogue that the findings note references.
data/raw/infrastructure/*.geojson
