# Python
*.pyc
__pycache__/
.ipynb_checkpoints/

# System
.DS_Store

# Data (keep only final)
permits_raw.csv
housing_projects_filtered.csv
*_refreshed_*.csv

# Generated outputs
*.db
outputs/*.html
outputs/*.md
data/outputs/
data/backups/
databases/
output/D5/
output/D6/

# Scrape outputs and run artifacts (regenerable from scripts in scripts/)
data/raw/accela_inspections/
data/raw/accela_processing_status/
data/raw/accela_record_status/
data/raw/accela_status/
data/raw/accela_url_discovery/
logs/

# Audit run outputs (regenerable from scripts in analysis/audit_2026-05-16/)
analysis/audit_2026-05-16/*.txt
analysis/audit_2026-05-16/*.csv

# Accela scrape experiment HTML responses (large; regenerable)
experiments/accela_scrape/response_*.html

# Letter drafts (keep private; use docs/letters/sent_log.md for metadata)
docs/letters/*.md
!docs/letters/sent_log.md

# Keep structure
!outputs/.gitkeep

berkeleyshops-audience/
hello-fly/
.obsidian/

# Obsidian per-user state
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Large binary exports
*.zip
*.m4v
*.mov
*.mp4

# Exception: deployed tour videos (compressed for web)
!docs/videos/*.mp4

# Tour source recordings (large uncompressed files, keep local only)
data/raw/tour_recordings/*.m4v
data/raw/tour_recordings/*.mov
data/raw/tour_recordings/*-source.mp4

# PDFs — managed via document mirror subsystem (IA + R2), not git
pdf/
*.pdf

# OS cruft
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# SQLite auxiliary files
*.db-journal
*.db-wal
*.db-shm

# Pending review — do not commit until notebook is reconciled
notebooks/MASTER_ANALYSIS.ipynb

# Accela authentication cookies (never commit)
experiments/accela_scrape/cookies.txt

# Inspection scraper output — machine-generated, regenerated by future runs
data/raw/accela_inspections/

# URL discovery scraper output — machine-generated, regenerated by future runs
data/raw/accela_url_discovery/
