# dedup47 — collapse duplicate file-row finaled events (Step A, audit record)

**Date:** 2026-06-29 · **Who:** CC, gated + confirmed by John.
**What:** 4 permits had **two** finaled-`new_unit`-master events each (from the CPRA two-file overlap +
within-file duplicate rows), so their units were counted **twice** in the CO total. Demote the duplicate
finaled-master event → `subsidiary`/`net_units 0`, keeping ONE (the `MIN(event_id)`). **Net −47 CO.**
**Why:** the dedup-contamination audit (`scratch/2026-06-29/dedup_contamination_audit.py`) found v4 dedups
permit KEYS but not EVENTS — these 4 are the only *counted-completion* permits with doubled finaled-masters.

## The 4 permits
| permit | finaled-master events (before) | dates | net each | extra removed |
|---|---|---|---|---|
| **B2014-05786** | 2 | 2021-08-31 **/ 2021-08-31** (same date — within-file dup, file A) | 44 | **−44** |
| B2018-03576 | 2 | 2020-01-10 / 2025-08-12 (different) | 1 | −1 |
| B2019-01241 | 2 | 2020-03-04 / 2023-10-23 (different) | 1 | −1 |
| B2020-03494 | 2 | 2022-09-07 / 2023-09-22 (different) | 1 | −1 |

**Total −47.** Each now counts ONCE. ⚠ Note 3 of 4 have **different finaled dates** — they are the same logical
permit recorded with different finaled dates across the two files (not same-date dupes); collapsing one copy is
still correct for the count. This different-date fact is load-bearing for Step B's dedup-key design.

## Discipline / trace
ADR-002 VERDICT layer; demote duplicate finaled-master → subsidiary/0 (keep `MIN(event_id)`). Single
transaction, **rowcount==1 per permit** (guard: each had exactly 2 finaled-masters pre-write), all-or-nothing,
snapshot-restore on any failure. Fresh-conn verify (each now 1 counted finaled-master) PASS. Idempotency: re-run
**0 rows**. Real before/after CO (2018-2025): **3,723 → 3,676 (−47)**.

**Snapshot:** `databases/keep_snapshot_2026-06-29_pre-dedup47.db` (integrity ok, size-matched).
**sha:** pre `9cb9471658e13281` → post **`899993e290e06717`**.

## Reconciliation state (now dedup-clean)
```
3,066 + C2 1,036 − Shattuck 163 − ADU-tail 17 − C-multifamily 199 − dedup47 47 = 3,676 vs city 4,022 (−346)
```
The −47 removes the masking over-count; **−346 is the honest dedup-clean figure** (the +147 held UNDER side
would bring it to ~−199). The phase collapses remain valid (distinct permits, single finaled events — verified).

## Reverse
Restore `keep_snapshot_2026-06-29_pre-dedup47.db`, or re-promote each demoted duplicate:
`UPDATE event_classifications SET housing_role='new_unit', net_units=<44|1> WHERE event_id=<the demoted id>`.

*Not committed to git. v4 mutation only; this record is the audit trail.*
