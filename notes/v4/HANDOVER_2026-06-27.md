# HANDOVER — Berkeley Housing Pipeline (chat-Claude session, 2026-06-27)

**Purpose:** Hands this work to a NEW chat-Claude session. Read whole, then ask John where things stand
before proposing. This SUPERSEDES the 2026-06-26 handover's *progress* section (which froze at "JN-D
bijection recon done"). The settled architecture and canonical facts in `CLAUDE.md` still hold. A
matching TECHNICAL handover (disk/artifact state) is produced by CC — read both; this one is decisions
and strategy, CC's is on-disk ground truth.

**Roles (unchanged):** chat-Claude plans/fact-checks/writes prompts, NEVER edits the repo. CC executes
all filesystem/DB/git on John's machine. John owns all irreversible ops and is the ONLY bridge moving
chat-Claude's `/mnt/user-data/outputs/` files into the repo. dev branch only; no commit/push without
John's instruction.

---

## THE ONE THING TO UNDERSTAND FIRST (this session's pivotal finding)

We came in to do the v4↔HCD ADU bijection. We leave having discovered that **the ADU work, though
correct and complete, is one of FOUR corrections needed to reconcile v4's full APR to the city's
filing — and is NOT the biggest one.** A full-APR state check (CC, end of session) found v4 currently
reproduces **CO 3,066 vs city 4,022 (~24% under)** and **BP 4,911 vs city 4,531 (~8% over, mostly
year-scramble)**. A four-corrections sizing then showed the reconciliation CLOSES to ~3.5%, and that
the dominant lever is **multifamily count recovery, not ADUs.**

**The four corrections, sized and ordered by impact (the marching orders):**

| # | correction | dir | units | nature | recoverable? |
|---|---|---|---|---|---|
| **C2** | **multifamily count-gap** | +CO | **+864** | ~16–23 large apartment buildings dropped to ZERO because UnitsAdded was blank (flagged-not-guessed). **Biggest lever, ~1.9× the relabel.** Lands in deep under-years (2020 +254, 2022 +166). | 16/23 from WorkDescription regex ("83 dwelling units"); only 7 need Accela |
| **C1** | **584 ADU relabel** | +CO | +457 | the hardened relabel set (442 finaled), even spread 2018–2025 | YES — in hand |
| **C3** | **phantom-master (#3)** | −CO | −163 real (−231 upper) | **essentially ONLY 1951 Shattuck** (two 163u permits, one building, CY2024). Rest is tiny ADU-pair/ancillary noise. | needs #3 building-identity pass (but Shattuck is a known special case) |
| **C4** | **BP reporting-year** | ~0 | year-shuffle | ~67% of the BP gap is reporting-year-vs-event_date reassignment; only ~33% (+380) true excess | reporting-year map; alignment not magnitude |

**Net arithmetic:** −956 current gap; C1+C2 (+1,321) − C3 (−224) ≈ +1,097 → v4 ~4,163 vs city 4,022,
**~3.5% overshoot**. The corrections roughly close the cumulative CO gap; residual is small and
year-lumpy. **The APR capstone is achievable** — it was not, on raw numbers, before this sizing.

**Why this reorders everything:** the highest-yield, cheapest correction (C2, mostly description-regex)
was invisible until we reconstructed the FULL APR. The session's ADU depth was the branch; C2 is the
trunk's biggest hole. **Lead the trunk reconciliation with C2.**

---

## STRATEGY (decided this session — the destination everything now points at)

**Primary near-term target: Councilmember Shoshana O'Keefe** (District 5 — North Berkeley + the Hills,
i.e. ADU country). She is THREE audiences in one person: a **Berkeley High teacher** (can adopt the
curriculum), a **housing-focused councilmember**, and an **11-year former ZAB chair** (understands
permit data from the inside; helped approve 10,000+ units). This collapses the journalist/academic/
city-staff sequencing problem — she is a single ally-doorway, and because she's a housing champion the
"city undercounted its own ADUs" finding lands as GOOD NEWS she can use (more RHNA units), not as an
accusation. (Web-verified June 2026: D5 councilmember, term to 2028, sokeefe@berkeleyca.gov.)

**The deliverable for her: a 4-notebook curriculum arc ending in an APR-generator capstone.**
1. *Why permits aren't buildings* (worldview seed — events primary, entities constructed; from JN-A).
2. *Classify housing roles* (the centralized `classify`; from JN-C).
3. *Triangulate against oracles* (the oracle-independence lesson; the ADU bijection as worked example;
   the PRINCIPLE doc made runnable).
4. *CAPSTONE: generate the APR and check it against the city's filing.* The integration payoff a former
   ZAB chair recognizes instantly. **Reframed (John's call): the capstone teaches the THREE-GAP
   DECOMPOSITION** ("here's the city's number, ours, and the three traceable reasons they differ") —
   which is a stronger, more honest civic-data lesson than a number that merely matches.

**Hard dependency:** the capstone's credibility = "rebuilt from scratch, here's how it reconciles to
the city." So the capstone GATES on the trunk reconciliation (C1–C4) being done. You cannot teach
"and here's how it reconciles" until it does. Build the first 3 notebooks in parallel (they don't need
final numbers); the capstone comes after the reconciliation.

**Audience readiness (honest):** Journalists — ready after relabel + JN-D (they need a checkable
finding; you'd be over-building if you wait for the curriculum). Goldman/academic — ready after the
curriculum (method + reproducibility). City staff — technically ready after reconciliation BUT add the
deferred "collaborative discrepancy-framing" carry-forward item first; best reached LAST, via O'Keefe,
after Goldman/press warm it up.

---

## WHAT WAS SETTLED THIS SESSION (do not re-litigate)

**1. Classifier centralized + committed (commit aa6ded0, dev).** The v4 `housing_role` classifier was
trapped as a cell-string inside `build_jn_c.py` (un-importable; the file crashed on load via a hardcoded
`/home/claude/` path) — the ACUTE form of the "drift pattern" John named in the June 18 audit. It is now
lifted into `scripts/housing_rules/permit_role.py` (importable, with `classify` + `net_units` + vocab +
**25 anchored tests** = 16 vocab + 9 deflation). Proven **behavior-identical on all 85,793 events (0
mismatches)**; JN-C role distribution unchanged (alteration 64,739 / subsidiary 9,597 / ambiguous 7,403
/ new_unit 2,964 / demolition 920 / non_housing 170). All consumers re-pointed to `from housing_rules
import classify`; exec-extraction eliminated. `classifiers.py` left untouched (stays RHNA-only).
**STANDING RULE reinforced:** the classifier lives in `housing_rules`; the notebook IMPORTS and renders
it, never redefines it. Generator (.py) is source of truth; never hand-edit the .ipynb.

**2. v4↔HCD ADU bijection (JN-D engine, verified; not yet a notebook).** `scripts/v4/build_jn_d.py`
runs end-to-end, five HARD asserts green (anchor 842, regression 649, match 839, hardened 584, band
531–584). **KEY FINDING: 99.6% any-role coverage — the ADU gap is FLAGGING, not coverage.** Of 842 HCD
ADU parcels, v4 has a permit on 839; only 3 genuinely missing. The 704 "under-other-role" split via the
settled classifier → **584 true new_unit** (the keyword 977 hardened down — the verify-a-zero caught
~40% companions: solar/REV/demo). Band 531–584 (49 multi-permit APNs for #3); 441 finaled / 143 not.
Oracles carried as columns: HCD + Assessor Imps + footprints. **The reversible relabel (584 →
event_classifications) is NOT yet done — it is C1 of the reconciliation.**

**3. Oracle-independence principle (written; `/mnt/user-data/outputs/PRINCIPLE_oracle_independence.md`,
presented, not yet placed in repo).** Rank oracles by independence; concordant (HCD) vs
independent-mechanism (Assessor, footprints, utilities). **Empirically proven this session:**
footprint=1 is 44.6% FALSE on real detached ADUs (Building Outlines layer lags the build-out), so
**footprint is a one-directional POSITIVE oracle only — dissent is dead.** "Silence ≠ negative" and
"corroborate, never source" are the load-bearing invariants. Oracle access settled: HCD + Assessor +
Berkeley building-footprints (`gis.cityofberkeley.info/.../Building_Safety/MapServer/7`, join via
`to_canonical_apn` on space-form PARCELID) all LIVE; Address Points live but format unconfirmed;
utility meters + recorded deed-restrictions are named-only (portal/CPRA-gated). Berkeley ≈29,119
parcels; 61.5% have exactly 1 footprint (1 structure is the NORM).

**4. Two-axis confidence scorer (calibrated across TWO independent implementations; prototype only,
not persisted).** Validated by a parallel build (chat-Claude `prototype_score_v2.py` + CC
`prototype_score_v2_cc.py`) that agreed at ρ=0.90/0.85 on raw scores — the architecture is real, not a
coding artifact. Reconciled `prototype_score_v3.py`. **The model:**
   - **TRUNK new_housing_conf** ("net new dwellings, any kind?") — interior: unit-count, multifamily,
     ADU, new_unit. Oracle: **Imps only**.
   - **BRANCH adu_conf** ("specifically an ADU?") — interior: ADU/conversion/legalization, small,
     detached/R-3. Oracles: **HCD + footprint≥2 only**. **BOUND: adu_conf ≤ new_housing_conf.**
   - **Invariants:** role set by `classify` only (oracles never set/flip it); oracles agree-or-abstain,
     NONE subtract; per-axis oracle attachment.
   - **FIVE settled decisions:** D1 explicit medium band (uncertainty surfaced = harvest queue); D2
     weak-only converts floor to LOW (false-positive guard); D3 pre-2017 statutory terms
     ("second/secondary unit") behind a creating-context guard (recovered 9 real, rejects existing-unit
     refs); D4 unchanged regressions (679 high/high core, big buildings low-adu, forbidden corner
     EMPTY); **D5 (just decided, CC to build) — route classifier-`ambiguous` + ADU-language to
     adu-MEDIUM = the real "classifier-unsure" ADU harvest queue** (guard: bare-ambiguous with no ADU
     language stays low; weak-only stays low).
   - **NEXT on scorer:** CC builds D5 and re-runs; then chat-Claude writes the SCORING SPEC (June-25
     style design doc) as the scorer's source-of-truth — the scorer is otherwise DONE.

---

## THE PLAN (sequenced)

**A. Finish the scorer:** CC builds D5, re-runs v3, confirms the medium tier reads as genuine
classifier-unsure ADU cases (not weak-only/noise). Then chat-Claude writes the scoring spec. → scorer DONE.

**B. The TRUNK RECONCILIATION (the core, sized above) — one push, C2-led:**
   1. **C2 multifamily count-gap** — recover the 16 description-derivable building counts (regex the
      unit count from WorkDescription); 7 need Accela. Biggest + cheapest lever.
   2. **C1 584 relabel** — reversible write to event_classifications (ADR-002 VERDICT layer). Use the
      scorer's three-way split: relabel high, REVIEW medium (the D5 queue), exclude low. **First op
      that mutates v4 — its own gate.**
   3. **C3 #3 / Shattuck** — handle 1951 Shattuck (the −163 special case) + the small ADU-pair residue.
      The general phantom-master discriminator can stay deferred; the APR needs the special case.
   4. **C4 BP reporting-year map** — align event_date → reporting-year so BP comparisons are meaningful.

**C. Build the 4-notebook curriculum** (notebooks 1–3 in parallel with B; capstone after B).

**D. The deliverables that ride on a reconciled APR:** ADU map ("build in my back yard," all ADUs
coded by confidence band — transparency as armor), then approach O'Keefe.

---

## DEFERRED / PENDING JOHN'S DISPOSITION (untouched)
- The general **#3 phantom-master discriminator** (building-identity model) beyond the Shattuck special
  case — and the **June 25 probabilistic building-identity model** (`building_identity_probabilistic_
  model.md`, uncommitted, designed-not-built). NOTE: the scoring layer we built this session is the
  CLASSIFICATION-confidence cousin of that BUILDING-IDENTITY model — same weighted-voting philosophy,
  deliberately kept separate (John's call: classification first, identity stays pending).
- **Pushing dev beyond local** (lift is committed at aa6ded0, unpushed).
- **Capacity JN** (footprint÷lot screening for underbuilt parcels → infill potential). Trap pre-flagged:
  it's lot-COVERAGE not FAR; ADUs are FAR-exempt under CA law; needs current BMC Title 23 + lot-area
  join; it's a SCREENING hint not a buildability determination. Post-core.
- **Collaborative discrepancy-framing** (the standing carry-forward item) — prerequisite for any
  city-staff-facing claim.

## DISCIPLINE THAT EARNED ITS KEEP THIS SESSION
- **LOOK before building** caught: the classifier wasn't in housing_rules (it was trapped); footprint
  dissent was 44.6% false; the APR was 24% low for reasons the relabel doesn't fix; C2 > C1 by 2×. Every
  one of these would have been a wrong build if assumed. SIZE before sequencing.
- **Parallel implementation as an oracle:** chat-Claude + CC independently built the scorer; agreement
  validated the design, divergences localized the exact decisions to make explicit. Repeat this pattern
  for high-stakes logic.
- **The verify-a-zero:** the keyword 977 → hardened 584 caught ~40% false relabels. Never trust a
  keyword gate; run the settled classifier.
- **CC is the more careful coder** (John's standing assessment) — give CC implementation; chat-Claude
  plans, fact-checks, writes prompts, and does NOT jump to coding (it did twice this session; John
  corrected it — the parallel-build pattern is the right channel when chat-Claude does write code).
- Compare LIKE QUANTITIES (units-to-units); an overshoot is not automatic success — name WHICH units.
