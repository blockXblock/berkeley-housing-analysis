# HANDOVER 2026-07-01 — for a fresh chat-Claude

**How to start:** read THIS → `CLAUDE.md` → `PROGRESS.md`, then continue without re-deriving. Everything
below is committed on `dev` (verify with `git log`; the meta-discipline is *verify, don't trust summaries* —
including this one). Current: `dev` HEAD `fd8a539`, last pushed to `origin/dev` = `7ada490` (1 unpushed).

## The session arc (what got built)
- **JN-E** (`scripts/v4/build_jn_e.py` → `notebooks/v4/JN-E_co_reconciliation.ipynb`): the CO reconciliation as a
  **reproducible, baseline-gated** notebook — derives CO 3,676 vs city 4,022 (−346) + BP 3,945 from v4+CKAN,
  asserts == `data/baselines/reconciliation_baseline_2026-06-29.json` (both-ways gate). Full data-flow +
  verifiability annotations + 3 viz (waterfall / gap-Sankey / lineage).
- **JN-H** (`build_jn_h.py` → `JN-H_document_harvester.ipynb`): the harvester acquisition MAP, correcting a
  prior misframing (**B-permit harvest is the DEFAULT, not a missing feature** — only `harvest_plansets.py:167/174`
  is ZP-specific). Momentary-link + triage-heuristic knowledge captured. + viz.
- **All 5 v4 notebooks visualized** (JN-A/C/D/E/H), text-sandwiched + derive-from-data + mislead-guards.
  JN-D viz is a **companion** (`build_jn_d_viz.py`) gated to the engine's EXP anchors — the engine is untouched.
- **JN-A verifier restored** (`verify_jn_a_conservation.py`): re-anchored to invariant + documented-deltas
  (was false-failing post-dedup). PASSES.
- **Assessed-value data committed** (`62d2554`, on dev): 672 projects, working+served — the property-tax feature
  is feature-complete on dev (`45617b2`+`5530ff3`+`62d2554`), pending the main deploy.
- **FIVE conventions written to CLAUDE.md:** investigation-JN (consolidate scratch→committed JN); derive-against-
  external-timestamped-baseline (never hardcode the answer); commit-autonomy (CC commits approved work on dev,
  irreversible ops gated); visualization (viz cells, derive-not-hardcode, viz-verifiability); anchor-to-invariants
  (anchor checks to what stays true, not what legitimately moves — invariant − documented deltas).

## The architecture factoring (how to think about the whole)
- **Two foundational pipeline SETS:** **SPINE** = the universal METHOD (ingest → dedup → classify → phase-collapse
  → ancillary-demote → reconcile); **CALIBRATION** = the city-specific DATA the methods read (`corrections/v4/`).
- **Consumer layers** sit on the spine: **serving** (v2 → berkeleybuild.com / APR), **journalist-access**
  (README Colab demo, methodology pages).
- **Curriculum = the spine READ PEDAGOGICALLY** (not a separate set) — it teaches the same methods, one generation
  behind (see curriculum finding).
- **Transformers vs consumers:** transformers CHANGE the data (the correction writes); consumers READ it (JN-E,
  serving, curriculum). Keep them distinct.

## ⚠ THE REPRODUCIBILITY GAP (critical, next-tier)
**The notebooks are NOT a from-raw pipeline.** From raw, JN-A→85,793 (JN-A does NO dedup) and JN-C→~3,066-era
classification. The corrections that reach 82,923 / 3,676 (event-dedup + C2/C3/C-multifamily/dedup47) live as
**out-of-notebook `scratch/2026-06-2*/*.py` gated writes** — **JN-E RECONCILES the result but does NOT APPLY them.**
To close: a **JN-B dedup** stage + a **JN-corrections** stage between JN-A and JN-E, each a **universal method that
READS a calibration file** from `corrections/v4/`, applied in order (**C-multifamily after C2-T2** — the one
coupling), with **HELD items encoded as hold-not-apply** (esp. the +147).

## THE ENDANGERED INPUTS — ✅ NOW PRESERVED (correcting the prior prompt's "not yet done")
Done this session (`7ada490`): the load-bearing correction SOURCE is now a tracked store **`corrections/v4/`** —
`c2_count_recovery.csv` + `c3_tail_demote_list.json` (were untracked scratch, one cleanup from lost) + the 3
externalized permit lists (`dedup47_permits.csv`, `c3_shattuck_collapse.csv`, `c_multifamily_collapse.csv`, were
inline literals) + a `README.md` documenting applied-vs-held + universal-vs-local. The correction notebooks will
READ these. (Prior prompt said "NOT yet done" — stale; verified tracked.)

## ⚠ THE HAZARD (do not destroy the corrected DB)
**JN-A and JN-C are DESTRUCTIVE to the live `berkeley_housing_v4.db`.** JN-A re-ingests → rebuilds events to
pre-dedup 85,793; JN-C `DELETE+INSERT event_classifications` → wipes the C2/C3/C-multifamily/dedup47 corrections.
**Never run them against the live DB** (only a snapshot, for a from-scratch test). **Safe-to-run read-only:
JN-E, JN-D-viz, JN-H, verify_jn_a** (JN-D engine is read-only too but heavy + a GIS network call).

## THE CURRICULUM FINDING (align, not rebuild)
See `notes/curriculum_development/2026-07-01_v4_alignment_not_rebuild.md` (+ the PROGRESS pointer). The curriculum
already teaches behavioral (JN4); the gap is address-as-identity + v2/v3 numbers + inline spine. Reframe address-key
as the planted limitation the student rediscovers (JN6b already plants a limitation). **Deep-read must check the
active `scratch/jn{2,3,4,6b}_rewrite/` prototypes — they may already reflect the v4 turn.** Open: confirm JN3
group-by-address + whether JN6b's planted limitation is building-identity-related.

## THE +147 (the held under-count — how to ground it independently)
`docs/audit/2026-06-28_c_multifamily_collapse_write.md` + JN-H §4: **B2021-03302 (69)** is effectively R2-grounded
(2352 Shattuck = **proj179**, plan sets already in R2 + investigated: South=69). **B2018-03422 (55, 2503 Haste)**
+ **B2016-05139 (23, 2740 San Pablo)** need a targeted Accela scrape — via the **Building-native engine**
(`url_discovery_scraper` defaults Building), NOT a from-scratch build. Provenance: count from the building's own
doc (`source_document_id`), never the city APR.

## OPEN THREADS
- **+147 Accela harvest** (JN-H is the map) · **104 building-identity adjudication** (47 real-ties + 28 cross-APN
  + 25 large-cluster + 4 residual; input to the committed building-identity module) · **~−150 residual**
  (contested-direction, needs per-permit independent proof).
- **JOHN's (main-side, irreversible):** assessed-value → main deploy (cherry-pick `45617b2`+`5530ff3`+`62d2554`,
  purge `explorer.{html,js}`+`explorer_data.js`, NO regen — data in 62d2554) + the **dev↔main reconciliation**
  (curriculum is MAIN-ONLY; merge `origin/main` into dev). See *Site / publish state* in PROGRESS.
- **Curriculum alignment** (the finding above) · **the JN-B/JN-corrections build** (closes the reproducibility gap).

## THE META-DISCIPLINE (apply it to CC summaries AND memory/PROGRESS)
**Verify, don't trust summaries.** This session it caught, each time by reading the actual artifact: the harvester
"needs-a-B-permit-fix" misframing (it's the default); JN-A's true nature (hand-authored pioneer, no generator, has
a conservation verifier); JN-D "is a notebook" (it's a run-only engine); the JN-A verifier silently false-failing
post-dedup; a convention that "landed" but never committed; the curriculum being "a failed entity course" (it's
already behavioral). A fresh Claude should treat CC's summaries, memory, and even PROGRESS as **hints to verify
against the code/DB/git**, not ground truth — count rows, read the file, `git log` before asserting.
