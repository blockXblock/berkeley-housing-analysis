# HANDOVER 2026-07-02 — for a fresh chat-Claude

**How to start:** read THIS → `CLAUDE.md` → top of `PROGRESS.md`, then continue without re-deriving.
Everything below is committed AND PUSHED (`dev` == `main` == `origin` at `3b6720a` unless a later session
moved it — `git log` is truth). Meta-discipline unchanged: **verify, don't trust summaries — including this one.**

## The headline (what changed today)
**The CO reconciliation is nearly closed: 3,970 vs city 4,022 = −52** (UPDATED same evening: El Jardin
resolved +55 — see below), with **+23 HELD** (San Pablo no-docs only) and **~−29 unexplained**
(was −346 this morning; −956 in June).
And **the pipeline is reproducible from raw**: `JN-A → JN-B (dedup) → JN-C (classify) → JN-F (corrections)`
rebuilds the exact live DB (completion-set equality, 0 diffs) in ~20s. Current baseline:
`data/baselines/reconciliation_baseline_2026-07-02c.json` (co 3,970 / gap −52 / sha 72fc7b54bc42800b).

## The session arc (2026-07-02, one long CC session)
1. **Triage:** HEAD was import-broken since June-28 (`rhna_credit_cycle` source never committed) — fixed
   `de5da7e`. June housekeeping committed; D6/D7 timestamp churn reverted.
2. **dev↔main reconciled + assessed-value DEPLOYED.** The "curriculum is MAIN-ONLY" note was stale
   (double-committed duplicates). berkeleybuild.com now serves assessed-value. **Ops facts:** no Cloudflare
   purge is ever needed; Pages wedged for ~4h on a GITHUB INCIDENT (their status went yellow) — we switched
   to workflow-based Pages (`.github/workflows/pages.yml`, actions bumped to Node-24 majors) during it;
   both changes are keepers.
3. **Reproducibility gap CLOSED:** `scripts/v4/stage_methods.py` (THE importable stage-method home) +
   `build_jn_b.py`/`build_jn_f.py` → JN-B/JN-F notebooks; JN-C parameterized (live-DB refusal guard — the
   wipe hazard is now enforced by code). **8-angle review found 14 confirmed defects, all fixed** (the big
   one: rc==1 guards weakened to rc<=1 in the lift → restored as verify-or-halt). Calibration hardened:
   `corrections/v4/{event_dedup_holds,held_items,calibration_checksums}.json`.
4. **+147 harvest (JN-H map):** B2021-03302 (Logan South) grounded at **69** from the proj179 plan set
   (sheet A.08, R2). **El Jardin (B2018-03422): the city's 55 matches NOTHING in the building's own record**
   (0 dwelling units / 254 beds / 193 rooms — a GLA) → held on a CONVENTION question (see the CIC brief
   below). San Pablo (B2016-05139): NO digital docs exist in Accela (post-retry, master+4 subs+UP2006 empty).
5. **The 4x-reinvestigation lesson:** Logan South had been established FOUR times (May-19, June-4 v2 write
   "NumberUnits=69 permit-stated", June-28 held-as-city-only, today) — because knowledge lived in prose.
   **All three held counts sat in our own `NumberUnits` field all along.** The durable fix:
   **`corrections/v4/grounded_counts.csv`** — the per-permit ADJUDICATION LEDGER (count + source document +
   corroboration), applied by `apply_grounded_counts`, refusing still-held permits, never overwriting.
   The ledger IS the anti-re-derivation memory.
6. **NumberUnits Stage-1 pass (John-approved):** 71 ambiguous finaled events with NumberUnits≥3 partitioned →
   **2 real recoveries: Logan Park NORTH = 135** (entirely uncounted — WAS the bulk of the "~−150 residual")
   **+ Acheson Commons Bldg B = 35** ("35 NEW RES UNIT" in its own description; C2's new_unit filter never
   saw it). Field verdict: **triage+corroborator for new construction ONLY, never auto-count** (alterations
   carry building-size; validation 14/15 vs C2 prose). Stage-2 classifier lift DECLINED. Adjudication record:
   `data/audit/numberunits_stage1_2026-07-02.csv` (incl. phase-subsidiaries that would have double-counted).

## OPEN THREADS (ranked next-step recommendation in PROGRESS + chat)
- ~~El Jardin kitchen question~~ **DONE same evening (CIC): in-unit kitchens confirmed → co-living
  DWELLING units; the 55/193/254 conflict was units/bedrooms/beds of ONE building; city was RIGHT
  (mis-enumeration hypothesis retracted); +55 applied via the ledger (`407ce37`).**
- **~−29 tail** — now enumerable permit-by-permit vs city rows (the Logan-North-style triage).
- **Audit Findings page + JN-G revision watcher** (the agreed visibility direction — start once −29 lands).
- **Full-city BP stream** → un-hold the RHNA bar (council audience).
- **Curriculum alignment** (+ today's 4x-reinvestigation arc as capstone material).
- Smalls: regenerate JN-H (Planning-first routing + empty-grid memos — recorded in the audit doc, not yet
  in the notebook); JN-E → import stage_methods metrics; building-identity module commit; San Pablo records
  request; the 104 building-identity adjudications.

## ⚠ Standing hazards / facts
- JN-A/B/C/F all default to a THROWAWAY target and REFUSE the live DB (env `PIPELINE_DB_PATH` or per-stage
  `JN_X_DB_PATH`). Notebook toolchain python: `/opt/miniconda3/envs/jupyter_env/bin/python`.
- Legitimate result change = calibration row + gated write + **APPEND a new timestamped baseline** (proven
  twice today), never a hand-edit.
- Key audit doc for today: `docs/audit/2026-07-02_147_harvest_findings.md` (incl. write traces).

---

## CIC BRIEF — El Jardin ✅ EXECUTED 2026-07-02 EVENING (kept for the method pattern — do NOT re-run)

**Task:** Determine the physical unit configuration of the residential building at **2501-2509 Haste St /
2433 Telegraph Ave, Berkeley CA** (operating name likely "El Jardin"; 6-7 stories, completed ~2020,
ground-floor retail, ~254 residents).

**The one question that matters: do the residential quarters have IN-UNIT KITCHENS?**
- If it's ~55 shared APARTMENTS (e.g. 4-5 bedrooms each, kitchen in each apartment, rented by the bed) →
  they are dwelling units; the city's APR count of 55 is right and we count 55 (convention-flagged co-living).
- If it's no-kitchen DORM/SRO-style rooms (shared communal kitchens) → true group-living; we count sleeping
  rooms + the manager's dwelling instead (a different, larger number ~193), and the city's 55 is a divergence.

**Where to look:** the operator/leasing website (search "El Jardin Berkeley apartments"), apartments.com /
Zillow / student-housing listings (floor plans, "X bed / Y bath" descriptions, kitchen photos), the
developer's project page. Berkeley ZAB/planning pages are fine for context but we already have those.

**What to bring back:** (1) kitchens in units — yes/no, with the URL + a quoted line or screenshot;
(2) if visible, the number of distinct apartments/suites and their bedroom mix; (3) the operator/manager name.

**What NOT to do:** don't treat the city's APR/CKAN numbers as the answer (that's the number under test);
marketing material settles the CONVENTION question (kitchens or not) — the count itself will be grounded
from permit/plan records. Deliver findings as text; CC will move them through `corrections/v4/` (the
grounded_counts ledger or held_items) with provenance.
