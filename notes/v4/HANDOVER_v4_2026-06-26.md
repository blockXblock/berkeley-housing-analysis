# HANDOVER — Berkeley Housing Pipeline v4 (chat-Claude session, 2026-06-26)

**Purpose:** This document hands the v4 work to a NEW chat-Claude session. Read it whole, then ask John
where things stand before proposing anything. The older `RESUME_chat-claude_v4.md` is now STALE — it
freezes at "JN-A complete, JN-C not built." This document supersedes it for current state. The settled
architecture in RESUME and the canonical facts in `CLAUDE.md` still hold; only the *progress* section
is out of date.

**Roles (unchanged):** chat-Claude plans/fact-checks/writes prompts, NEVER edits the repo. CC executes
all filesystem/DB/git ops on John's machine. John owns all irreversible ops and is the ONLY bridge
moving chat-Claude's presented files into the repo (CC cannot see /mnt/user-data/outputs/; chat-Claude
cannot see John's disk). dev branch only, no push/commit without John's instruction.

---

## WHERE WE ACTUALLY ARE (the part the old resume doesn't have)

**JN-A is done, committed, pushed (cbcdeee).** 85,793 events from 32,202 CPRA rows, four-axis,
conserved, independent verifier PASS. This is settled. Do NOT re-run or re-litigate it.

**JN-C (pass 1) is BUILT, RUNS CLEAN, and COMMITTED + PUSHED (latest commit e4896bf).** It has had
TWO fixes since first build (deflation + dedup, both verified). This is the live work.
- Generator: `scripts/v4/build_jn_c.py` (committed — the SOURCE OF TRUTH for the notebook; the .ipynb
  is its build artifact; chat-Claude's sandbox copy is `build_jn_c_v2.py`). Notebook:
  `notebooks/v4/JN-C_classify.ipynb`. EDIT THE GENERATOR, regenerate, never hand-edit the .ipynb.
- It does #1 (housing/non-housing, description-first, generous-inconclusive) + #2 (master-collapse on
  permit family). DEFERS #3 (phantom-master discriminator). Writes ONLY `event_classifications`
  (reversible, overwrite-idempotent) + `output/jn_c_harvest_queue.csv`. Reads v2.db READ-ONLY for the
  has-documents bridge. Never touches events or v3.
- **16 vocabulary tests + 9 deflation-fix tests PASS** — anchored to real prior-research cases (2641
  College siding→alteration, Durant temp-power@83→ambiguous, ADU conversions/legalizations→new_unit,
  ADU-flagged kitchen remodel→ambiguous, movable home with/without permanence markers; deflation tests:
  blank SFR/ADU→1, blank multifamily→flagged-None, real counts preserved). Halts if any fail. Compiles
  warnings-as-errors, zero escaping bugs.

**Latest JN-C run results (CLEAN — deflation + dedup fixes applied, verified vs independent count):**
- Role distribution (85,793 events): alteration 64,739 · subsidiary 9,597 · ambiguous 7,403 ·
  new_unit 2,964 · demolition 920 · non_housing 170. (Healthy; stable across fixes — fixes touch
  net_units/counting, not roles.)
- Completions BY UNITS vs v3 (deduped by permit; floor=confident new_unit, ceiling=+ambiguous-finaled):
  CY2024 v4 floor 865 vs v3 708 · CY2025 589 vs 497 · 8-yr total **3,019** (ceiling 3,113) vs v3 4,310.
- **KEY READING:** CY2024's 865 includes the known +163 deferred 1951 Shattuck double-count; corrected,
  CY2024 ≈ 702 ≈ v3 708 — i.e. v4 and v3 AGREE on the settled year once the one known identity case is
  accounted for. The remaining 8-yr gap (3,019 vs 4,310, ~1,290) is concentrated in EARLY YEARS
  (2018-2021 floors are thin: 2018=34, 2020=71) = real completions parked in v4's ambiguous/harvest
  bucket that v3 counted (sometimes via its imperfect best-permit-pick). The gap IS the harvest queue.
- Size-band distribution (deduped): 1-unit(SFR/ADU) 637/600 · 2-4 26/53 · 5-19 4/51 · 20-99 25/1,448 ·
  100+ 6/867. Mid-rise + 100+ bands verified as real nameable Berkeley buildings (inflation check).
- Multifamily count-gap: 23 confident new_unit permits finaled with NULL net_units (apartments missing
  a unit count — flagged not guessed; a small secondary harvest target).
- Harvest queue: 1,954 inconclusive permits finaled 2018-2025; all have NO R2 doc (the 37 R2 docs map
  to confident/other permits) → all need Accela document-fetch.

## OPEN FINDINGS (the live diagnostic thread — this is where work resumes)

**1. DEFLATION bug — FIXED (committed e4896bf), verified.** The 1-unit band had 640 projects / 405
units (blank UnitsAdded on confident SFR/ADU summing to 0). Fix: confident `new_unit` with blank/null
UnitsAdded → 1 IF single-dwelling (SFR/ADU/duplex-or-smaller); if MULTIFAMILY (apartment/N-story/5+),
stays null-and-FLAGGED (count-gap, not guess-1). Result: 1-unit band 405→600 units; 23 multifamily
count-gap permits flagged. 9 deflation tests pass.

**2. INFLATION check — DONE, band is clean except one deferred case.** No REV inflation anywhere (0
children with units > master; REV/DEF children correctly excluded from sums — the v3 cumulative-
restatement bug is NOT present in v4). Mid-rise (20-99) band = 25 distinct real nameable Berkeley
buildings. 100+ band = 6 listed but 5 distinct buildings because of finding #3 below.

**3. DEDUP bug — FIXED (committed e4896bf), verified vs independent count.** The comparison/band queries
counted per finaled-EVENT without deduping by permit, so cross-file-overlap + within-file-duplicate
permits double-counted (+47u). Fix: a temp view `_jnc_finaled_permits` collapses to one finaled event
per distinct `source_record_key` before aggregating. Floor 3,066→3,019, mid-rise 26/1,492→25/1,448 —
matches CC's independent dedup reconstruction EXACTLY. (JN-A correctly preserved duplicate rows; dedup
belongs at the JN-C counting layer, never at ingestion.)

**4. ⚠ KNOWN DEFERRED — 1951 Shattuck phased double-count (the first concrete #3 case).** APN
057 204600100 (1951 Shattuck Ave) has TWO new_unit masters: B2019-05608 (Phase 1, 163u) + B2021-04893
(Phase 2, 163u) — ONE physical building counted twice = **+163u inflation in CY2024 / the 100+ band**.
This is NOT a dedup issue (they are distinct permits, distinct source rows) — it is the building-IDENTITY
problem (#3 phantom-master) surfacing: phased permits = one building, not catchable by REV/DEF
permit-family collapse. DEFERRED to the #3 build (John's call). **When reading CY2024: subtract 163 →
~702 ≈ v3's 708.** Do NOT silently trust the uncorrected 865. The S1.5 spec already flagged 1951
Shattuck; it is the canonical phased-building test case for #3.

**5. v4↔HCD ADU bijection — RECON DONE, build is the NEXT live step.** Goal: locate the early-year
harvest housing (finding-3 reading: the 8-yr gap is real completions in v4's ambiguous bucket). Only v4
(724 ADU permits, 659 APNs) and HCD (`table_a2.UNIT_CAT='ADU'`, ROW-LEVEL, 1,363 rows / 1,160 addresses
/ 846 APNs, stable ~140-190/yr) have a real ADU determination. v3 stores NO adu flag (implicit, never
persisted); v2 has NO adu flag (only description LIKE '%ADU%', 583 — weak proxy). So the real comparison
is **v4 vs HCD** (oracle), v2 weak-third, v3 absent. HCD has MORE ADUs than v4 confident — same direction
as the deflation/early-year gap; likely real ADUs sitting in v4's harvest queue. The bijection LOCATES
which HCD ADUs v4 missed / deflated / over-claimed → harvest priorities.
  - **Join discipline (from CLAUDE.md — critical):** use `housing_rules.to_canonical_apn(raw,county)`
    (alphanumeric/structure-preserving), the 3-LAYER cross-walk, NOT bare strip-non-digits (gave 890/892
    false-dead). APNs are NOT stable (re-platting orphans the join) — a "v4 missing" may be an APN-renumber
    artifact. v4 raw_address is BLANK; pull from raw_payload (StreetNumber/StreetName/StreetType). Report
    MATCH RATE first before reading buckets.
  - **SHADOW vs ADU-PAIR rule (from CLAUDE.md):** never address-collapse — a main-house+ADU on one lot is
    TWO real permits/COs, which v4's permit-keying correctly sees as two. Collapsing reproduces the
    erasure bug that already nearly killed 3 real ADUs.
  - **HCD is ORACLE ONLY** — compare, surface divergence, NEVER tune v4 toward HCD (circular).
  - Proposed buckets per HCD ADU address: agree / v4-has-but-ambiguous (→ harvest priority, HCD confirms
    real) / v4-has-but-0-units (deflation residue) / v4-missing-entirely (gap or APN-renumber) /
    v4-has-but-HCD-doesnt (v4 over-claim OR a Berkeley under-report — the inverse finding).

## SETTLED CLASSIFIER VOCABULARY (do not re-derive — John approved each)
- Priority-order rules (first match wins): (1) -REV/-DEF → subsidiary; (2) Demolition → demolition;
  (3) Sign → non_housing; (4) movable/tiny-home → new_unit ONLY with permanence markers (wheels
  removed/permanent foundation) else ambiguous; (5) **ADU=Yes REQUIRES description corroboration**
  (ADU/conversion/legalization language) → new_unit, else ambiguous (the dirty-flag case); (6)
  New+housing-language → new_unit; (7) New+units>0+no-disqualifier → new_unit; (8) New unconfirmed →
  ambiguous; (9) Alteration+conversion/legalization language → ambiguous; (10) plain alteration →
  alteration (units floored 0); (11) blank/unmatched → ambiguous (generous).
- `net_units` is PROSE-BLIND (from UnitsAdded, never description) and master-only.
- **ADU reality (web-verified):** ADUs are added via garage/basement CONVERSION (Alteration work-type)
  or LEGALIZATION (AB 2533, near-zero construction) — so Work Type=New is a good CORROBORATOR but a BAD
  GATE (it misses conversions). Description gates; New supports. Movable/tiny homes count only with
  permanence markers (wheels removed, permanent foundation), else inconclusive. ADU=Yes corroboration =
  description (best) OR Work Type=New; OccType=R-3 is a tiebreaker; UnitsAdded is EXCLUDED as a
  corroborator (the Durant temp-power@83 lesson).
- **Completion rule = clean v4 rule:** a building completes in the year its master New permit's
  permit_finaled event fires. NO best-permit-pick fallback (that was v3's Rule-C, source of wrong-picks).
- **Size bands (Berkeley-real, web-verified):** 5 units = legal multifamily/density-bonus/affordability
  line (appears 3 independent places); 2-19 = Berkeley's explicit "middle housing" band; real large
  projects are 100+ (110/160/169/240 in the news). Bands: 1 / 2-4 / 5-19 / 20-99 / 100+.

## KEY PAYLOAD/SCHEMA FACTS (confirmed against the real v4.db this session)
- v4 events.raw_payload keys (27): PermitNumber, Submittal/Issuance/Finaled/Completed Date (+ *Status
  siblings, correctly UNMATCHED), Work Type, WorkDescription, ADU, OccType, SubType, UnitsAdded,
  UnitsRemoved, Detached, CO Required, JobValuation, Parcel Number (space-form e.g. '052 155600100'),
  StreetNumber/StreetName/StreetType. All values are strings or None (ingested dtype=str) — cast in code.
- v4 events.raw_address is BLANK; raw_apn carries the space-form APN; source_record_key = PermitNumber.
- v2 documents table: 2,040 rows, only 75 with r2_url (architect_plans/ + affordability_forms/ for MAJOR
  projects, NOT the small-ADU tail). Keyed by project_id (+ often-blank permit_number).
- v2 projects: canonical_address / normalized_address (NO apn column), merged_into_id (soft-retire —
  follow to survivor). The has-documents bridge joins documents.project_id → projects, uses
  canonical/normalized_address + direct documents.permit_number, follows merged_into_id.
- R2 base: pub-2cee87f70da64080ab70ee0a34b55099.r2.dev. No full bucket manifest (only a stub).

## IMMEDIATE NEXT STEPS (in order)
1. ✅ DONE — Checkpoint (JN-C committed) + deflation fix + inflation check + dedup fix, all committed
   (latest e4896bf) and pushed. Handover updated to here.
2. **▶ NEXT LIVE STEP — v4↔HCD ADU bijection.** Build it with the join discipline in finding #5
   (canonical APN via `to_canonical_apn` + 3-layer cross-walk, address-from-raw_payload,
   match-rate-FIRST, NO address-collapse per the ADU-pair rule, HCD oracle-only). Output: which HCD ADUs
   v4 missed / has-but-ambiguous / deflated / over-claims → the harvest-queue priority list. This LOCATES
   the early-year gap (the ~1,290-unit 8-yr shortfall = real completions in v4's ambiguous bucket).
3. (After the bijection) Harvest-RESOLUTION: relabel the HCD-confirmed ambiguous ADUs to confident
   new_unit (reversible relabel — re-run JN-C's classifier or a targeted decision layer), watch v4's
   floor rise toward v3 in the early years. The harvest queue (1,954) all need Accela document-fetch;
   only 37 R2 docs exist (major projects). Accela sweep is queue-first/aimed, can be FAST (rate-limiting
   was self-imposed, limited only once); reuse existing harness (inspection_scraper.py,
   experiments/accela_scrape/upload_harvest_to_r2.py).
4. (Deferred) **#3 phantom-master discriminator** (permit-family + building-label in WorkDescription,
   NOT APN). First concrete case is finding #4 (1951 Shattuck phased pair). Builds the `structure`
   projection; resolves phased-building double-counts (the +163). Multi-session work on fully-labeled
   evidence. It is REFINING LABELING (a relationship label between permits) + the structure projection.
5. (Deferred) THE SALVAGE conversation: re-point website/Explorer, curriculum (JN1-JN6b), and 4 months
   of APR work at the v4 spine. A MIGRATION, not abandonment.

## DISCIPLINE REMINDERS (the ones that bit us THIS session)
- LOOK, don't assume: when you need a fact about John's real data, have CC inspect it (read-only) BEFORE
  building against an assumed shape. This session, building JN-C against assumed payload keys hit a wall;
  one CC look settled it. Same for v2 schema, the documents table, the ADU recon.
- The generator (.py) is the source of truth; the .ipynb is its artifact. Fix the generator and
  regenerate — never hand-edit the notebook (it diverges from the generator and loses the next rebuild).
- Gate every regenerated notebook: compile-warnings-as-errors + run the vocabulary tests + check escaping
  bugs, BEFORE presenting.
- Verify the ACTUAL file on John's disk before trusting an error (CC's `resource` path names the real file).
- Compare LIKE QUANTITIES: this session's biggest analytical bug was comparing v4 building-COUNTS to v3
  UNIT-counts. Always check you're comparing units-to-units before crying wolf over a divergence.
- An overshoot is NOT automatic success — identify WHICH units v4 counts that v3 didn't, and whether
  they're real (address-collapse recovery = good) or artifact (REV double-count = bad).
