# SUPERSEDED 2026-09-07 — dependencies now live in pyproject.toml + uv.lock.
#
# This file listed 14 packages while the real environments carried 114 and 239;
# it had drifted into fiction. The measured dependency surface is 29 packages,
# derived by AST-scanning every import in the repo (see
# notes/2026-09-07_python_env_consolidation_plan.md S1.1).
#
# Set up the environment with:
#     uv sync
#
# which builds .venv from uv.lock: Python 3.14.0, pandas 3.0.5.
# Kept as a pointer so anything looking for requirements.txt finds its way.
